version: '2'

services:

  rabbitmq:
    image: rabbitmq:3-management
    hostname: rabbithost
    ports:
      - 8080:15672

  test:
    image: java:8-jre
    links:
      - rabbitmq:rabbithost
    volumes:
      - ../source:/app
    command:
      "/app/bin/gameserver"
